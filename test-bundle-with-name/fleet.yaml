defaultNamespace: test-namespace
name: custom-bundle-name
# This custom name should break the UI link

helm:
  releaseName: test-app
  chart: "test-chart"
  repo: "https://charts.helm.sh/stable"
  version: "1.0.0"
